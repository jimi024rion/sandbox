# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
#

# go fmt
# golangci-lint
# mockry
# tbls
# swagg
# swagg fmt
# go test
# pushしてもよいの?チェック


pre-commit:
  parallel: true
  jobs:
    - name: jobsでスクリプトを実行する
      script: "prehook-test1"
    - name: jobsでrunを実行する
      run: go version
  scripts:
    "prehook-test1":
      runner: bash
    "test2.sh":
      runner: bash
  commands:
    makecheck:
      run: make hello
      glob: "Makefile"


    # - run: yarn eslint {staged_files}
#       glob: "*.{js,ts,jsx,tsx}"
#
#     - name: rubocop
#       glob: "*.rb"
#       exclude:
#         - config/application.rb
#         - config/routes.rb
#       run: bundle exec rubocop --force-exclusion {all_files}
#
#     - name: govet
#       files: git ls-files -m
#       glob: "*.go"
#       run: go vet {files}
#
#     - script: "hello.js"
#       runner: node
#
#     - script: "hello.go"
#       runner: go run
